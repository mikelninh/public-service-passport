{
  "name": "benefit-bridge-webmcp",
  "version": "0.4.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && node scripts/smoke.mjs",
    "pilot": "node scripts/run-authority-pilot.mjs"
  },
  "engines": { "node": ">=20" }
}
