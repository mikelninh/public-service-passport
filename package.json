{
  "name": "benefit-bridge-webmcp",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser-v1.mjs",
    "check": "npm test && node scripts/smoke.mjs && node --check public/v1.js && node --check public/openproof-verifier.js"
  },
  "devDependencies": {
    "playwright": "1.57.0"
  },
  "engines": { "node": ">=20" }
}
